legacy-peer-deps=true
# Fix para conflitos entre React 19 (Next.js 15) e bibliotecas legadas (Stripe, Radix, etc)
# Este arquivo garante que a Vercel instale as dependências sem erros de versão de pares.
